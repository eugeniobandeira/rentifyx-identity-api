apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: prod

patches:
  - path: patch-deployment.yaml
    target:
      kind: Deployment
      name: api
  - path: patch-hpa.yaml
    target:
      kind: HorizontalPodAutoscaler
      name: api
